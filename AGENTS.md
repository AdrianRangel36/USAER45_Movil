# Expo HAS CHANGED

Este proyecto usa **Expo SDK 54**. Consulta los docs de esa versión exacta antes de escribir
código: https://docs.expo.dev/versions/v54.0.0/

**NO actualices el SDK.** Está fijado en 54 a propósito, porque es la versión que soporta el
Expo Go instalado en los teléfonos del equipo. Ya se intentó con SDK 57 y 56: Expo Go decía
"versión incompatible" y no había actualización disponible que lo arreglara.

Antes de tocar nada, lee `CONTEXTO_APP_MOVIL.md`: tiene la arquitectura, las decisiones ya
tomadas y los contratos exactos de la API.
